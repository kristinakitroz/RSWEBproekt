@{
    ViewData["Title"] = "Create Professor Account";
}

<h2>Create Professor Account</h2>

<p class="text-muted">
    Teacher: <strong>@ViewBag.TeacherName</strong>
</p>

<hr />

<form asp-action="CreateAccount" method="post">
    <input type="hidden" name="id" value="@ViewBag.TeacherId" />

    <div asp-validation-summary="All" class="text-danger mb-3"></div>

    <div class="mb-3">
        <label class="form-label">Professor Email</label>
        <input name="email" type="email" class="form-control" placeholder="professor@rsweb.com" />
    </div>

    <button type="submit" class="btn btn-primary">Create Account</button>

    <a asp-action="Details"
       asp-route-id="@ViewBag.TeacherId"
       class="btn btn-secondary ms-2">
        Back
    </a>
</form>
